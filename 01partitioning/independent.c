#include "partitioning.h"
#include "colors.h"

int independent_output(Tuple *tuples, size_t n_tuples, size_t n_hash_bits, size_t n_threads, pthread_t* threads) {
    printf(COLOR_GREEN "Running independent partitioning..." COLOR_RESET "\n");
    return EXIT_SUCCESS;
}
